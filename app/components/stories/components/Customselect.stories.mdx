import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks'
import CustomSelect from '../../ch/components/CustomSelect.vue'

<Meta
  title="Components/CustomSelect"
  component={CustomSelect}
/>

export const Template = (args, { argTypes }) => ({
  props: Object.keys(argTypes),
  components: { CustomSelect },
  template: '<div> <CustomSelect :label="label" :options="options" :clearable="clearable" :searchable="searchable" :placeholder="placeholder" /> <br/><br/><br/><br/> <CustomSelect variant="negative" :label="label" :options="options" :clearable="clearable" :searchable="searchable" :placeholder="placeholder" /> <br/><br/><br/><br/>  </div>'
})


# CustomSelect
<Canvas>
  <Story
    name="Example"
    args={{
      label:"",
      variant:"outline",
      options:['De', 'Fr', 'It'],
      clearable:false,
      searchable:false,
      placeholder:"Select Text",
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

# Dependancy on Vue.js
This component is made based on vueselect library. You can find it here: https://vue-select.org/guide/install.html#yarn-npm
In order for this component to work you will need to import the css file. And the component itself in your vue project.

# Interaction and accessibility

Custom selects are used to select a single value from a list of options.

```html
<p class="meta-info"><span class="meta-info__item">
    This is a meta info
  </span><span class="meta-info__item">
    This is another meta info
  </span>
</p>
```

<a href="?path=/story/components-CustomSelect--example">
  Go to the Canvas Tab
</a>

---

<a href="?id=components-CustomSelect--example" target="_blank">
  Open page in full width in a new tab
</a>
